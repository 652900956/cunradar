# CunRadar 完整使用教程（多通道版 · 多 AI 适配）

> 一份从「这是什么」到「从零跑通并收到第一条推送」的完整保姆级教程。
> 适合非技术背景阅读：每一步都告诉你做什么、预期看到什么。

---

## 目录

- [〇、这个项目是干嘛的](#零项目是干嘛的)
- [一、准备工作](#一准备工作)
- [二、本地快速跑通（5 分钟）](#二本地快速跑通5-分钟)
- [三、自定义你要追踪的内容（关注列表）](#三自定义你要追踪的内容关注列表)
- [四、配置推送渠道（飞书 / 企业微信 / 钉钉）](#四配置推送渠道飞书--企业微信--钉钉)
- [五、配置 AI（多供应商可切换）](#五配置ai多供应商可切换)
- [六、部署到 GitHub Actions（自动化 + 定时）](#六部署到github-actions自动化--定时)
- [七、定时时间说明](#七定时时间说明)
- [八、工作原理（进阶，可选看）](#八工作原理进阶可选看)
- [九、常见问题排查](#九常见问题排查)
- [十、目录结构与配置速查](#十目录结构与配置速查)
- [十一、技术栈与许可证](#十一技术栈与许可证)

---

## 〇、这个项目是干嘛的

**一句话**：CunRadar 是一个「个人信息雷达」——你告诉它你关心哪些博主、项目、博客，它每天自动去爬这些来源有没有更新，用 AI 生成一份中文每日摘要，再**定时推送到你的飞书 / 企业微信 / 钉钉群**。

**它不是监控别人，而是帮你从信息洪流里筛出自己真正关心的内容。**

### 能做什么（功能一览）

| 功能 | 说明 |
|------|------|
| 🎬 **YouTube 频道追踪** | 订阅频道，自动获取最新视频 |
| 📺 **B站 UP 主追踪** | 关注 UP 主更新 |
| 📝 **博客 / RSS 订阅** | 任何支持 RSS 的博客或新闻源 |
| 💻 **GitHub 项目追踪** | 关注指定仓库的 commits / releases |
| 🔥 **GitHub Trending** | 每日热门仓库排行榜（前 N 名） |
| 🤖 **AI 智能摘要** | 用可切换的 AI 模型，把更新写成中文每日动态 |
| 📄 **HTML 日报** | 生成响应式网页报告，可选部署到 Cloudflare Pages |
| 📱 **多通道推送** | 每日定时推送到飞书 / 企业微信 / 钉钉（按配置，可全开） |

### 推送渠道（三选一或全开）

| 渠道 | 消息格式 | 签名/加签 |
|------|----------|-----------|
| 飞书（Lark） | 富文本（post） | 可选签名校验 |
| 企业微信 | Markdown | 无 |
| 钉钉 | Markdown | 可选加签 |

每个通道**独立**：在配置里填了哪个的 Webhook 就推哪个，留空自动跳过。

### AI 供应商（可随时切换，均 OpenAI 兼容）

| 供应商 | 模型（默认） | 费用 |
|--------|--------------|------|
| DeepSeek | `deepseek-v4-flash` | 付费（便宜） | 默认开启思考模式 |
| 智谱 GLM | `glm-4.7-flash` | **免费** |
| 腾讯混元 | `hunyuan-lite` | **免费** |
| 通义千问 | `qwen3.7-flash` | 便宜 |

### 整体架构（数据怎么流动的）

```
你配置的来源 ──┐
              ├─→ 采集器(YouTube/B站/RSS/GitHub) ──→ 去重(SQLite)
你配置的AI ───┤                                          │
              │                                          ▼
定时触发 ─────┘                                    有更新? ──→ AI 生成摘要 ──→ 生成 HTML ──→ 推送(飞书/企微/钉钉)
                                                         │
                                                      无更新
                                                         │
                                                         ▼
                                              推送「今日无新内容」(或兜底发最新一条)
```

---

## 一、准备工作

你需要：

1. **一个 GitHub 账号**（免费），用来托管代码 + 跑定时任务。
2. **一个飞书 / 企业微信 / 钉钉群**（至少有其一，用来收推送）。
3. **一个 AI 供应商的 API Key**（DeepSeek / 智谱 / 混元 / 通义 任选，前两个免费）。

本地电脑需要：

- **Git**（用来下载/提交代码）
- **Python ≥ 3.12**
- （推荐）**uv** 包管理器：安装命令 `pip install uv`，或见 https://docs.astral.sh/uv/

> 不想装环境也可以：本项目主要跑在 GitHub 云端（Actions），本地只是"第一次验证 + 改配置"用。

---

## 二、本地快速跑通（5 分钟）

### 步骤 1：获取代码

如果你本地已经有这个文件夹（如 `CunRadar-Feishu`），直接 `cd` 进去即可。否则克隆：

```bash
git clone https://github.com/652900956/cunradar.git
cd cunradar   # 本地文件夹名不同就换成实际目录（如 CunRadar-Feishu）
```

**预期**：当前目录下出现 `cunradar/`、`config/`、`README.md` 等文件。

### 步骤 2：安装依赖

**方式 A（推荐，用 uv）：**
```bash
uv sync --no-dev
```

**方式 B（用 pip）：**
```bash
pip install -e .
```

> 依赖包含：`requests`、`PyYAML`、`feedparser`、`beautifulsoup4`、`lxml`、`pytz`、`curl-cffi`。
> **预期**：命令执行完没有红色报错。

### 步骤 3：配置密钥（最关键的一步）

复制模板：
```bash
cp .env.example .env
```

用记事本 / VS Code 打开 `.env`，至少填两项（AI Key + 至少一个推送通道的 Webhook）：

```env
# 选一个 AI 供应商，并填入对应的 API Key
AI_PROVIDER=deepseek
AI_API_KEY=你的AI_key

# 飞书群机器人的 Webhook 地址（启用飞书推送就填）
FEISHU_WEBHOOK_URL=https://open.feishu.cn/open-apis/bot/v2/hook/xxxx
```

**预期**：`.env` 文件已保存，且都含有真实值（不是 `your_xxx_here`）。

> `.env` 已被 `.gitignore` 忽略，不会误提交到仓库，放心填。

### 步骤 4：本地运行

```bash
python -m cunradar
# 用 uv 时：uv run python -m cunradar
```

**预期**：控制台依次打印：
```
[采集] ...
[AI] Generating daily digest...
[Notify] Sending digest...
  [Feishu] 推送中...
  [Feishu] 推送成功（N 行）
```
并且你的飞书群里收到一条日报。第一次跑会追踪示例关注对象（村长的频道等），之后换成你自己的即可（见第三节）。

---

## 三、自定义你要追踪的内容（关注列表）

「推送什么」=「你关注了谁」。两类配置方式，**二选一**：

| 方式 | 改哪里 | 适合 | 隐私 |
|------|--------|------|------|
| **A. 改配置文件** | `config/config.yaml` 的 `follow:` 段 | 本地玩、不在意暴露订阅 | 会进仓库 |
| **B. 环境变量** `FOLLOW_CONFIG` | 本地 `.env` / 线上 GitHub Secrets | **推荐**，订阅不公开 | 不进代码 |

> 优先级：**B > A**。设了 `FOLLOW_CONFIG` 就以它为准，忽略 config.yaml 的 `follow`。

### 方式 A：直接改 `config/config.yaml`

文件里 `follow:` 段（约第 43 行起）长这样：

```yaml
follow:
  youtube:
    - name: "Web3村长"
      channel_id: "UC5MbekhrH8iyFBQLbccBSRg"
  bilibili:
    - name: "Web3村长Official"
      uid: 1224034462
  rss:
    - name: "村长博客"
      url: "https://cunzhangblog.com/rss.xml"
  github:
    - name: "CunRadar"
      repo: "652900956/cunradar"
  github_trending:
    enabled: true
    language: ""
    limit: 5
```

**添加关注 = 在对应分类下加一个 `-` 块；删除关注 = 删掉那个块。**

| 来源 | 必填字段 | 格式示例 |
|------|----------|----------|
| YouTube | `channel_id`（地址栏 `UC...`） | `- name: "UP主A"` <br> `  channel_id: "UCxxxxxxxxxxxx"` |
| B站 | `uid`（空间 `space.bilibili.com/` 后数字） | `- name: "UP主B"` <br> `  uid: 123456789` |
| RSS | `url` | `- name: "某博客"` <br> `  url: "https://example.com/rss.xml"` |
| GitHub | `repo`（`owner/repo`） | `- name: "某项目"` <br> `  repo: "owner/repo"` |
| GitHub Trending | 开关 | `enabled: true/false`、`language: ""`(全语言)、`limit: 5` |

**各来源 ID 怎么拿：**
- **YouTube channel_id**：打开频道主页，地址栏 `UC...` 那段就是。
- **B站 uid**：打开 UP 主空间，地址栏 `space.bilibili.com/` 后面的数字。
- **RSS**：博客通常 `/rss.xml` 或 `/feed`；不知道就搜「站点名 rss」。
- **GitHub repo**：`owner/repo` 格式。

### 方式 B：用 `FOLLOW_CONFIG`（推荐，一行 JSON）

本地放 `.env`、线上放 GitHub Secrets，优先级高于 config.yaml：

```env
FOLLOW_CONFIG={"youtube":[{"name":"Web3村长","channel_id":"UC5MbekhrH8iyFBQLbccBSRg"}],"bilibili":[{"name":"Web3村长Official","uid":1224034462}],"rss":[{"name":"村长博客","url":"https://cunzhangblog.com/rss.xml"}],"github":[{"name":"CunRadar","repo":"652900956/cunradar"}],"github_trending":{"enabled":true,"language":"","limit":5}}
```

> 想换关注列表时，**方式 A 要改完 `git push` 才对线上生效；方式 B 在 GitHub Secrets 里改完即可，不动代码**。

---

## 四、配置推送渠道（飞书 / 企业微信 / 钉钉）

三个通道相互独立。在 `config.yaml` 里 `notification` 段结构如下（**实际值通过环境变量注入，不写死在代码里**）：

```yaml
notification:
  feishu:
    webhook_url: "${FEISHU_WEBHOOK_URL}"
    secret: "${FEISHU_SECRET}"        # 可选
  wecom:
    webhook_url: "${WECOM_WEBHOOK_URL}"
  dingtalk:
    webhook_url: "${DINGTALK_WEBHOOK_URL}"
    secret: "${DINGTALK_SECRET}"      # 可选
```

### 1. 飞书（Lark）

1. 打开目标飞书群 → 右上角 **···** → **设置** → **群机器人** → **添加机器人** → 选 **自定义机器人（Webhook）**。
2. 起名（如「每日资讯雷达」），安全设置建议选 **签名校验**（可选，更稳）。
3. 复制 **Webhook 地址**，形如 `https://open.feishu.cn/open-apis/bot/v2/hook/xxxxxxxx-xxxx`。
4. 填到 `.env`（线上填 GitHub Secrets）：
   ```env
   FEISHU_WEBHOOK_URL=上面复制的Webhook地址
   FEISHU_SECRET=                     # 开了签名校验就填 secret，没开留空
   ```

### 2. 企业微信

1. 打开目标企业微信群 → 右上角 **···** → **群机器人** → **添加机器人** → 复制 **Webhook 地址**，形如 `https://qyapi.weixin.qq.com/cgi-bin/webhook/send?key=xxxxxxxx`。
2. 企业微信机器人**无需签名**，直接填：
   ```env
   WECOM_WEBHOOK_URL=上面复制的Webhook地址
   ```

### 3. 钉钉

1. 打开目标钉钉群 → 右上角 **···** → **智能群助手** → **添加机器人** → **自定义机器人（Webhook 接入）**。
2. 复制 **Webhook 地址**，形如 `https://oapi.dingtalk.com/robot/send?access_token=xxxx`。
3. 若「安全设置」选了 **加签**，把生成的 **secret** 也记下来：
   ```env
   DINGTALK_WEBHOOK_URL=上面复制的Webhook地址
   DINGTALK_SECRET=                   # 选了加签就填，否则留空
   ```

### 推送内容长什么样

- **飞书**：一条**富文本（post）消息**——标题 + AI 摘要 + 各来源更新（带可点击链接）+ 可选「完整日报」网页链接；过长自动截断。
- **企业微信 / 钉钉**：一条 **Markdown 消息**——标题 + AI 摘要 + 各来源更新（带链接）+ 可选「完整日报」链接。

> 安全建议：Webhook 地址 = 「往群里发消息的钥匙」，**不要提交到公开仓库**。本地放 `.env`（已被忽略），线上放 GitHub Secrets。

---

## 五、配置 AI（多供应商可切换）

所有 AI 供应商都**兼容 OpenAI 的 `/chat/completions` 接口**，代码只用一套逻辑，切换靠配置。

### 内置预设（改 `AI_PROVIDER` 即可）

| `AI_PROVIDER` | 供应商 | 模型（默认） | 费用 | 备注 |
|---------------|--------|--------------|------|------|
| `deepseek` | DeepSeek | `deepseek-v4-flash` | 付费（便宜） | 质量稳定，推荐默认（旧名已停用） |
| `zhipu` | 智谱 AI | `glm-4.7-flash` | **免费** | 地址 `https://open.bigmodel.cn/api/paas/v4` |
| `hunyuan` | 腾讯混元 | `hunyuan-lite` | **免费** | 地址 `https://api.hunyuan.cloud.tencent.com/v1`；更好的免费档 `hunyuan-turbos-latest` |
| `qwen` | 通义千问 | `qwen3.7-flash` | 便宜 | 地址 `https://dashscope.aliyuncs.com/compatible-mode/v1`；若报模型名错误，改用 `qwen3.6-flash` / `qwen-turbo` |

### 怎么填

**方法一：只填 Provider（推荐）**
```env
AI_PROVIDER=zhipu
AI_API_KEY=你的智谱key
```
代码自动套用该供应商的接口地址和默认模型。

**方法二：自定义模型或接口（覆盖预设）**
```env
AI_PROVIDER=deepseek
AI_API_KEY=你的key
AI_MODEL=deepseek-v4-flash          # 想换模型就填（deepseek-chat/reasoner 已停用）；默认已开启思考模式
AI_BASE_URL=https://api.deepseek.com/v1   # 想换兼容端点就填
```
> 优先级：**环境变量 > config.yaml > 供应商预设**。设了 `AI_MODEL` / `AI_BASE_URL` 就以环境变量为准。

### 各供应商 Key 去哪拿

- **DeepSeek**：https://platform.deepseek.com → API Keys
- **智谱 GLM**：https://open.bigmodel.cn → 控制台 → API Key（注册即送免费额度）
- **腾讯混元**：https://cloud.tencent.com/product/hunyuan → 开通后获取 API Key
- **通义千问**：https://dashscope.console.aliyun.com → 模型服务灵积 / 百炼 → API Key

---

## 六、部署到 GitHub Actions（自动化 + 定时）

CunRadar 完全跑在 GitHub 免费额度上，**公开仓库**额度最宽松。部署后每天到点自动跑，你不用开电脑。

### 架构 / 费用

| 做什么 | 平台 | 费用 |
|-------|------|------|
| 采集 + AI 摘要 + 多通道推送 | GitHub Actions（定时） | ✅ 公开仓库免费 |
| 去重数据库 | GitHub Actions Cache | ✅ 免费 |
| HTML 日报网页（可选） | Cloudflare Pages | ✅ 免费计划够用 |

### 步骤 1：推送到你的仓库

本仓库假设你推到 `652900956/cunradar`（公开仓库）。首次推送：

```bash
git add .
git commit -m "init: CunRadar 多通道版"
git branch -M main
git remote add origin https://github.com/652900956/cunradar.git
git push -u origin main
```
> 若本地 `main` 与远端历史不一致：`git pull origin main --allow-unrelated-histories` 合并后再 `git push`。

### 步骤 2（可选）：创建 Cloudflare Pages 项目

想要「完整日报」网页才做：
1. 登录 https://dash.cloudflare.com → **Workers & Pages → Pages → Create project**。
2. 项目名填 `cunradar`（可自定义），**不要连接 git**，直接创建空项目。
3. 拿到项目名和域名（如 `cunradar.pages.dev`）。

> 不配 Cloudflare 完全没问题——推送照常工作，只是日报里没有「完整日报」网页链接。

### 步骤 3：配置 GitHub Secrets（必填项）

仓库 → **Settings → Secrets and variables → Actions → Secrets** → **New repository secret**：

| Secret 名称 | 必填 | 说明 |
|-------------|------|------|
| `AI_API_KEY` | ✅ 是 | AI 供应商的 API Key（兼容旧名 `DEEPSEEK_API_KEY`） |
| `FEISHU_WEBHOOK_URL` | ❌ 否* | 飞书机器人 Webhook（要推飞书就填） |
| `FEISHU_SECRET` | ❌ 否 | 飞书签名 secret（开了签名校验才填） |
| `WECOM_WEBHOOK_URL` | ❌ 否* | 企业微信机器人 Webhook（要推企微就填） |
| `DINGTALK_WEBHOOK_URL` | ❌ 否* | 钉钉机器人 Webhook（要推钉钉就填） |
| `DINGTALK_SECRET` | ❌ 否 | 钉钉加签 secret（选了加签才填） |
| `CLOUDFLARE_API_TOKEN` | ❌ 否 | Cloudflare API Token（要部署网页才填；不填跳过部署） |
| `FOLLOW_CONFIG` | ❌ 否 | JSON 关注列表（见第三节），优先级高于 config.yaml |

> \* 至少填一个推送通道的 Webhook，否则运行时会打印「未配置任何通道」并跳过推送。

**Cloudflare API Token 获取**：Cloudflare → My Profile → API Tokens → Create Token → 选 **Cloudflare Pages: Edit** 模板 → 复制 Token。

### 步骤 4：配置 GitHub Variables（可选）

仓库 → **Settings → Secrets and variables → Actions → Variables**：

| Variable 名称 | 说明 | 默认值 |
|---------------|------|--------|
| `AI_PROVIDER` | 选哪个 AI 供应商 | `deepseek` |
| `CUNRADAR_PUBLIC_URL` | 日报网页地址，如 `https://cunradar.pages.dev` | 空（无网页链接） |
| `CUNRADAR_PROJECT_NAME` | Cloudflare Pages 项目名 | `cunradar` |
| `FOLLOW_CONFIG` | 也可放这里（Variable 形式） | 空 |

### 步骤 5：手动触发一次，验证流水线

仓库 → **Actions → CunRadar Daily → Run workflow**。观察日志：
- 采集正常 → AI 摘要生成 → HTML 生成 → **推送成功** ✅
- 若某通道报 `code != 0` / `errcode != 0`，检查该通道 Webhook / 签名是否正确。

> 手动触发只验证一次；之后每天按第七节的时间**自动**运行。

---

## 七、定时时间说明

GitHub Actions 用 **UTC** 时间。本项目 `.github/workflows/daily.yml` 已按北京时间换算好：

| 北京时间 | UTC 时间 | workflow 里的 cron |
|---------|---------|-------------------|
| 06:30 | 22:30（前一天） | `30 22 * * *` |
| 12:20 | 04:20 | `20 4 * * *` |
| 21:00 | 13:00 | `0 13 * * *` |

**想改时间**：编辑 `daily.yml` 里 `on.schedule` 下的 cron（只改前两个数字：分钟、小时），其余保持 `*`。例如每小时跑：`"0 * * * *"`。改完提交推送即生效。

> GitHub 定时偶有 0~15 分钟延迟（官方调度负载），属正常；公开仓库不会因长期无活动被暂停。

---

## 八、工作原理（进阶，可选看）

1. **采集**：每个来源一个采集器，按 `follow` 配置拉取最近 N 小时的内容。
2. **去重**：拉到的内容用 `item_id`（如视频 ID、commit hash）存入 SQLite；已见过的不再推送。数据库放在 GitHub Cache，跨天累积。
3. **兜底**：某天某来源完全没更新时，首次见到该来源会兜底发一条「最新一条」，避免空跑；之后空闲日显示「无新内容」。
4. **AI 摘要**：把所有新内容拼成提示词，调用你配置的 AI 供应商，生成一段中文每日动态。
5. **报告**：同时生成一份 HTML 日报（可选部署到 Cloudflare）。
6. **推送**：按 `notification` 配置，把摘要+更新发到已启用的通道。

**想深度自定义？**
- 改「AI 怎么写摘要」：`cunradar/ai/__init__.py` 里的 system prompt。
- 改「飞书消息长什么样」：`cunradar/notification/__init__.py` 的 `_build_feishu_payload`。
- 改「采集哪些平台 / 怎么爬」：`cunradar/collectors/` 下对应文件。

---

## 九、常见问题排查

- **某通道收不到消息**：本机 `python -m cunradar` 看日志里 `[Feishu]` / `[WeCom]` / `[DingTalk]` 那行。报 `code != 0` / `errcode != 0` 多半是 Webhook 复制错；飞书/钉钉开了签名却没填对应 `SECRET` 也会失败。
- **AI 摘要为空 / 跳过**：日志打印 `[AI] Skipped (AI_API_KEY ... not configured)` 或 `[AI Digest] Failed`。检查 `AI_API_KEY` 是否填对、`AI_PROVIDER` 拼写、网络是否可达该供应商。
- **每天重复收到同样内容**：去重库在 GitHub Cache 跨运行累积。清过 Cache、或首次运行「兜底」发一条最新内容，属正常。
- **HTML 日报网页打不开**：确认配了 `CLOUDFLARE_API_TOKEN` 且 `CUNRADAR_PUBLIC_URL` 填对；没配则消息里无「完整日报」链接。
- **工作流一启动就失败（红色 ✗）**：多半是 `uses:` 的 Action 版本号写错。本项目已锁定真实最新版（`actions/checkout@v7.0.1`、`actions/setup-python@v7.0.0`、`astral-sh/setup-uv@v10.0.1`），不要手改成不存在的版本。
- **GitHub Actions 的 `if` 不能用 `secrets`**：这是平台限制。判断条件要用 `env` 上下文（如本项目部署步骤用 `if: ${{ env.CLOUDFLARE_API_TOKEN != '' }}`）。

---

## 十、目录结构与配置速查

```
CunRadar-Feishu/
├── .github/workflows/
│   └── daily.yml          # GitHub Actions 定时工作流（6:30/12:20/21:00 北京时间）
├── config/
│   └── config.yaml        # 关注列表 & 应用配置 & AI/推送配置
├── public/                # 网站图标等静态资源
├── cunradar/
│   ├── __main__.py        # 主入口 & 流程编排
│   ├── config.py          # 配置加载器（支持 ${ENV} 与 FOLLOW_CONFIG）
│   ├── ai/                # AI 摘要（多供应商预设）
│   ├── collectors/        # 各平台采集器
│   ├── storage/           # SQLite 去重存储
│   ├── report/            # HTML 日报生成
│   └── notification/      # 多通道推送（飞书/企业微信/钉钉）
├── .env.example           # 环境变量模板
├── pyproject.toml         # 项目配置
├── README.md              # 项目说明
└── 使用教程.md            # 本文件
```

**配置项速查表**

| 配置项 | 在哪设 | 作用 |
|--------|--------|------|
| `follow:` | `config.yaml` 或 `FOLLOW_CONFIG` | 追踪哪些来源 |
| `AI_PROVIDER` / `AI_API_KEY` / `AI_MODEL` / `AI_BASE_URL` | `.env` / Secrets / Variables | 选 AI 供应商与 key |
| `FEISHU_WEBHOOK_URL` / `FEISHU_SECRET` | `.env` / Secrets | 飞书推送 |
| `WECOM_WEBHOOK_URL` | `.env` / Secrets | 企业微信推送 |
| `DINGTALK_WEBHOOK_URL` / `DINGTALK_SECRET` | `.env` / Secrets | 钉钉推送 |
| `CLOUDFLARE_API_TOKEN` | Secrets | 部署网页（可选） |
| `CUNRADAR_PUBLIC_URL` / `CUNRADAR_PROJECT_NAME` | Variables | 网页地址/项目名 |
| 定时 `cron` | `daily.yml` | 每天运行时间 |

---

## 十一、技术栈与许可证

- **语言**：Python ≥ 3.12
- **AI**：OpenAI 兼容接口（DeepSeek / 智谱 / 混元 / 通义千问，可切换）
- **存储**：SQLite（GitHub Cache 跨运行持久化去重）
- **部署**：GitHub Actions + 可选 Cloudflare Pages
- **通知**：多通道 —— 飞书（post 富文本）/ 企业微信（markdown）/ 钉钉（markdown + 可选加签）

**License**：MIT
